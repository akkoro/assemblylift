module {{snake_case dns_name}}_dns {
    source = "./infra/{{{provider.name}}}/{{dns_name}}"

    // FIXME is it a problem that this can be different than the service-level module?
    providers = {
        {{platform.name}} = {{platform.name}}.{{platform.id}}
    }
}
